machine:
  Version: 
    ubuntu-14.04-XXL-1167-271bbe4
  python:
    version: 3.5.0

dependencies:

  override:
    - pip install --upgrade pip
    - pip install datalad
    - pip install pytest

test:

  override:
    - python -m datalad `which pytest` test.py